diff --git a/gaussian_renderer/__init__.py b/gaussian_renderer/__init__.py
--- a/gaussian_renderer/__init__.py
+++ b/gaussian_renderer/__init__.py
@@
-def render(viewpoint_camera, pc : GaussianModel, pipe, bg_color : torch.Tensor, scaling_modifier = 1.0, override_color = None):
+def render(viewpoint_camera, pc : GaussianModel, pipe, bg_color : torch.Tensor,
+           scaling_modifier = 1.0, override_color = None, opacity_modifier = None):
@@
-    opacity = pc.get_opacity
+    opacity = pc.get_opacity
+    if opacity_modifier is not None:
+        modifier = opacity_modifier.to(device=opacity.device, dtype=opacity.dtype)
+        if modifier.dim() == 1:
+            modifier = modifier.unsqueeze(-1)
+        if modifier.shape != opacity.shape:
+            raise ValueError(
+                "opacity_modifier must have the same shape as Gaussian opacity "
+                f"({tuple(opacity.shape)}), got {tuple(modifier.shape)}"
+            )
+        opacity = opacity * modifier.clamp(0.0, 1.0)
